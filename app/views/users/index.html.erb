<h1>Users</h1>
<ul>
  <% @users.each do |u| %>
  <li>id: <%= u.id %> --------> <%= link_to u.name, user_path(u) %></li>
  <% end %>
</ul>

<br><br><br>
<%= link_to 'Create New User', new_user_path %>
